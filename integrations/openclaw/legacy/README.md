# Legacy imported references

This folder stores OpenClaw-related reference material imported from the private `mempalace-openclaw` fork.

These files are not automatically treated as the current production path.
They are here so we can compare and reconcile earlier integration attempts while converging on a single canonical repository.
