# Private fork reference imports

This directory stores reference material imported from the private `mempalace-openclaw` fork.

These files are preserved for comparison and reconciliation.
They should not automatically be treated as the canonical implementation without an explicit merge decision.

Use them to:
- compare architecture decisions
- recover useful MCP/OpenClaw notes
- guide targeted migration into the canonical repository
